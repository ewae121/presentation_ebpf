# Presentation for EBPF

## Presentation compilation

The presentation use Marp. To install it, use:

```bash
make install
```

To build the presentation use:

```bash
make
```

The presentation will be available in **build/ebpf_presentation.html**